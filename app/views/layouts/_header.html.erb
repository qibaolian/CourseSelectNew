<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">

    <div class="navbar-header">
      <%= link_to "CourseSelect", root_path, class: 'navbar-brand' %>
    </div>

    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">

        <li class=<%= "active" if params[:controller]=="homes" %>> <%= link_to "主页", root_path %></li>
        <% if student_logged_in? %>
            <li class=<%= "active" if params[:controller]=="courses" %>> <%= link_to "我的课程", courses_path %></li>
        <% elsif teacher_logged_in? %>
            <li class=<%= "active" if params[:controller]=="courses" %>> <%= link_to "课程管理", courses_path %></li>
        <% elsif admin_logged_in? %>
            <li> <%= link_to "管理员面板", admin_path %></li>
        <% end %>
        <li class=<%= "active" if params[:controller]=="help" %>> <%= link_to "开发指南", help_path %></li>
      </ul>

      <ul class="nav navbar-nav navbar-right">

        <% if current_user %>
            <li>
              <%= link_to edit_user_path(current_user) do %>
                  <%= current_user.name %><span class="glyphicon glyphicon-user"></span>
              <% end %>
            </li>

            <li>
              <p class="navbar-text"><%= $Department.values_at(current_user.department)[0][:dept_name] %></p>
            </li>

            <li>
              <%= link_to sessions_logout_path, method: :delete do %>
                  退出 <span class="	glyphicon glyphicon-off"></span>
              <% end %>
            </li>

        <% else %>

            <li><%= link_to sessions_login_path do %>
                  登陆 <span class="	glyphicon glyphicon-user"></span>
              <% end %>
            </li>


        <% end %>
      </ul>
    </div>
  </div>
</nav>




